final int sizex = 500;
final int sizey = 500;

void settings(){
    size(sizex,sizey);
}

void setup(){
    //Need to determine size of grid, this can be complicated as images vary a lot in size.
    //Probably rule out areas we cant reacht
}

void draw(){
    // Determine square to act on
    // Do jumps in grid size alongside y
        //Do jumps alongisde grid size in x
            //Save indexes for later use in duples
            //Grab pixels on an empty array for simplification.
            //Rotate values and then use index reference to replace in the image.
            //Basically save index position and rgba values rotate but maintain index.
            //Save buffer to image.
}

//Good luck future Isaac :)
